# 🛠️ Технические инструкции и Разделение IT-задач

Этот документ разделяет технические задачи между **Python Lead (Иван)** и **WordPress Outsource Specialist**.

---

## 🐍 Иван (Python Lead / Tech Owner)
**Фокус:** Автоматизация, Генерация данных, Валидация, Контроль.
**Инструментарий:** Python, Selenium/Playwright, OpenAI API.

### 🎯 Твоя стратегия:
Ты не верстаешь страницы руками. Ты пишешь код, который:
1.  **Генерирует** идеальные данные для сайта (Schema.org, контент).
2.  **Мониторит** результаты (позиции, ответы AI).
3.  **Проверяет** работу аутсорсера (авто-тесты).

### 📋 Твои задачи:

#### 1. Генерация Schema.org (Python)
Вместо ручного написания JSON, используй скрипт `automation/schema_generator.py`.
*   **Задача:** Доработать скрипт, чтобы он брал данные из конфига (или Excel от Нини) и генерировал готовые `<script>` блоки.
*   **Output:** Файлы `schema_faq.json`, `schema_product.json`, которые ты передашь аутсорсеру.

#### 2. Мониторинг AI (Python)
Используй `automation/ai_monitor.py` (который мы обновили).
*   **Задача:** Запускать скрипт раз в неделю.
*   **Output:** Отчет для директора: "Нас стали чаще упоминать в ChatGPT".

#### 3. Валидация работы Аутсорсера (Python)
Напиши простой скрипт (используя `BeautifulSoup` или `extruct`), который:
*   Заходит на сайт `dimkava.ge`.
*   Парсит `<head>`.
*   Проверяет, что JSON-LD, который ты передал, реально там есть и не сломан.
*   **Почему это круто:** Ты принимаешь работу не "на глаз", а авто-тестом.

#### 4. Постановка ТЗ для Аутсорсера
Ты — технический заказчик. Ты формулируешь задачу на понятном языке, прикладываешь готовые файлы (код, картинки) и требуешь результат.

---

## 🌐 WordPress Outsource Specialist
**Фокус:** Верстка, Плагины, Настройка CMS, Frontend.
**Инструментарий:** WordPress, PHP, CSS, Elementor/Divi.

### 📋 Задачи для специалиста:

#### 📦 Блок 1: Страница FAQ
**ТЗ:**
1.  Создать страницу `/faq/` (и переводы `/en/faq/`, `/ka/faq/`).
2.  Реализовать функционал "Аккордеон" (раскрывающиеся вопросы).
3.  **Важно:** Использовать плагин совместимый с WPML/Polylang для переводов.
4.  **Дизайн:** Адаптивный, в стилистике основного сайта.

#### 📦 Блок 2: Внедрение Schema.org (Техническое SEO)
**ТЗ:**
1.  **Global:** Вставить код `LocalBusiness` (который пришлет Иван) в `<head>` главной страницы.
2.  **FAQ:** Вставить код `FAQPage` (от Ивана) на страницу FAQ.
3.  **Products:** Настроить WooCommerce (или текущий каталог), чтобы он автоматически подтягивал `Product Schema` (цена, наличие, валюта).
    *   *Если это невозможно стандартными средствами, вставить кастомный код в `functions.php`.*

#### 📦 Блок 3: Виджеты
**ТЗ:**
1.  Вставить карту Google Maps (iframe) на страницу Контакты.
2.  Добавить кликабельные ссылки на соцсети и мессенджеры.

---

## 🤝 Процесс взаимодействия

1.  **Иван** запускает скрипт -> получает `schema_faq.json`.
2.  **Иван** пишет тикет аутсорсеру: *"Привет, вот готовый JSON-код для FAQ. Вставь его в Head страницы /faq/."*
3.  **Аутсорсер** вставляет код.
4.  **Иван** запускает проверочный скрипт -> `Test Passed`.
5.  **Задача закрыта.**
